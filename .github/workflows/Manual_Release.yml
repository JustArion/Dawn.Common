# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         fallout --generate-configuration GitHubActions_Manual_Release --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Manual_Release

on:
  workflow_dispatch:
    inputs:
      Version:
        description: "Version"
        required: true

permissions:
  contents: write

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
          lfs: true
      - name: 'Cache: ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props', '**/packages.lock.json') }}
      - name: 'Setup: .NET SDK'
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
      - name: 'Restore: dotnet tools'
        run: dotnet tool restore
      - name: 'Run: TaggedRelease'
        run: dotnet fallout TaggedRelease
        env:
          Version: ${{ github.event.inputs.Version }}
          NugetPAT: ${{ secrets.NUGET_PAT }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: packages'
        uses: actions/upload-artifact@v5
        with:
          name: packages
          path: artifacts/packages
